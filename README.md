# BEV-tool
A Python tool that creates an inverse perspective mapping (BEV) from camera 2 and the lidar sensor of the Kitti dataset.
To run the script, you need to update the path to the dataset (ROOT) in main.

